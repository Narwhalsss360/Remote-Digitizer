<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:pf="clr-namespace:System.Windows;assembly=PresentationFramework"
                    xmlns:sys="clr-namespace:System;assembly=netstandard"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Color x:Key="MainWindowBackgroundColor">SlateGray</Color>
    <Color x:Key="AlternateBackgroundColor">DarkSlateGray</Color>
    <Color x:Key="MouseOverBackgroundColor">DimGray</Color>
    <Color x:Key="ForegroundColor">LightGray</Color>
    <Color x:Key="ActiveForegroundColor">Linen</Color>
    <Color x:Key="DisabledColor">DarkGray</Color>
    
    <sys:Double x:Key="DefaultWindowWidth">500</sys:Double>
    <sys:Double x:Key="DefaultWindowHeight">300</sys:Double>
    <sys:Double x:Key="TitleBarHeight">18</sys:Double>
    <sys:Double x:Key="StatusBarHeight">15</sys:Double>
    <pf:CornerRadius x:Key="MainWindowBorderCornerRadius">10</pf:CornerRadius>
    <pf:GridLength x:Key="TitleBarHeightGridLength">18</pf:GridLength>
    <pf:GridLength x:Key="StatusBarHeightGridLength">15</pf:GridLength>

    <Style x:Key="CommonWindowStyle" TargetType="Window">
        <Setter Property="Width" Value="{StaticResource DefaultWindowWidth}" />
        <Setter Property="Height" Value="{StaticResource DefaultWindowHeight}" />
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="AllowsTransparency" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="ResizeMode" Value="NoResize" />
    </Style>

    <Style x:Key="MainWindowBorderStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="{StaticResource MainWindowBorderCornerRadius}" />
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="{StaticResource MainWindowBackgroundColor}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground">
            <Setter.Value>
                <SolidColorBrush Color="{StaticResource ForegroundColor}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultButtonStyle" TargetType="Button">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" CornerRadius="3" BorderThickness="1" BorderBrush="Gray">
                        <Border.Background>
                            <SolidColorBrush Color="{StaticResource AlternateBackgroundColor}" />
                        </Border.Background>
                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" Margin="4" />

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup>
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="Border"
                                            Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                            To="{StaticResource MouseOverBackgroundColor}" Duration="0:0:0.2" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
